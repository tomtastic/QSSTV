Fix build #32: Create non-const copies for all openImage calls

Build #32 failed due to additional openImage calls in txwidget.cpp that
pass const QString & to a function expecting QString & (non-const reference).

Fixed in txwidget.cpp:
- setPreviewWidget(): Create non-const copy before calling openImage
- setImage(): Create non-const copy before calling openImage

This completes the fix for all openImage calls affected by the const
reference refactoring. All functions that pass const QString & parameters
to openImage now create non-const copies first.

Related files already fixed:
- extviewer.cpp (fixed in commit 563e775)
